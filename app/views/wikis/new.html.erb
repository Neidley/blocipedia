<h1>New Wiki</h1>

<div class="row">
  <div class="col-md-4">
    <p>Guidelines for wikis</p>
      <ul>
        <li>There are no rules.</li>
        <li>Dogs rule.</li>
        <li>Cats want to take over the world.</li>
      </ul>
  </div>
  <div class="col-md-8">
    <%= form_for @wiki do |f| %>
      <div class="form-group">
        <%= f.label :title %>
        <%= f.text_field :title, class: 'form-control', placeholder: "Enter wiki title" %>
      </div>
      <div class="form-group">
        <%= f.label :body %>
        <%= f.text_area :body, rows: 8, class: 'form-control', placeholder: "Enter wiki body" %>
      </div>
      <%= render partial: 'form', locals: { f: f, wiki: @wiki }%>
      <% if current_user.admin? %>
        <%= f.label :collaborators %>
        <%= f.collection_select(:collaborator_ids, User.all, :id, :email, {class: 'foo'}, {multiple: true})%>
      <% end %>
      <div class="form-group">
        <%= f.submit "Save" %>
      </div>
    <% end %>
  </div>
</div>
