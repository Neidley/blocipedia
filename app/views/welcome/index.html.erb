<div class="jumbotron">
  <h1>Blocipedia</h1>
  <p>Social, Markdown Wikis</p>
</div>
<div class="text-center">
  <h3>Create and share Markdown Wikis</h3>
</div>
<br>
<div class="column pull-left">
  <div class="col-md-4 text-center">
      <h4>Create as many public wikis as you want!</h4>
      Free Plan
  </div>
</div>
<div class="column pull right">
  <div class="col-md-4 pull-right text-center">
    <h4>Create private wikis!</h4>
    <div>
      <% if current_user.role == "premium" %>
        <%= link_to "Cancel Premium Plan", users_update_path(current_user), method: :update, class: 'btn btn-danger', data: {confirm: 'Are you sure you want to downgrade to standard?'} %>
      <% end %>
    </div>
    <div>
      <% if current_user.role == "standard" %>
        <%= link_to "Upgrade to Premium Plan", charges_new_path, class: 'btn btn-success' %>
      <% end %>
    </div>
  </div>
</div>
